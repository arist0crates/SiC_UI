var camera, cameraControls, scene, renderer, light, ambientLight, music;

main();
animate();

function main() {
	var World = {
		renderer,
		scene,
		camera,
		cameraControls,
		light,
		ambientLight,
		music
	  }
}